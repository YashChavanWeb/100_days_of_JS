// this loop is a method for array

const fruits = ['apple', 'mango', 'banana', 'grapes']

fruits.forEach((fruit) => {
    console.log(fruit)
})

// Note:
// - for each has a return value and which is by default undefined
// - even if we return anything on our own, then too it will return undefined only

